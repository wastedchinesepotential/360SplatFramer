/**
 * 360Splatter — Main entry point
 * Free browser-based 360° video to Gaussian Splatting frame extractor
 */

import './style.css';
import { initUI } from './ui.js';

// Initialize the app when DOM is ready
document.addEventListener('DOMContentLoaded', () => {
  initUI();
});
